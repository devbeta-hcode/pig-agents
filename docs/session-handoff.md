# Session handoff

Last updated: 2026-06-26

## Last session

- **2026-05-31** — Greenfield desktop repo: `PLAN.md` (master plan), `AGENTS.md`, security/dependency docs, Cursor rules, npm workspaces (`core`, `electron`, `renderer`), Electron main + preload IPC shell, minimal React renderer, platform helpers. No code imported from `pig-agents-web/`.
- **2026-05-31** — Replaced the rejected "loopback Express bridge" with a fully native IPC architecture:
  - **core**: deleted `api/` (Express routers) + `bridge/` (HTTP+ws server); ported all route logic into pure functions in `app/core/src/services.ts` (files, fs browse/rename, command, git, settings, ollama/openai model lists, chats, checkpoints, policy, approvals, sessions, diff revert). `browser/session.ts` is now a Playwright-free stub. `index.ts` exports `services` + streaming primitives (`runAgent`, `subscribeToSession`, `subscribeAgentCommands`, `workspaceWatcher`, `createPty`). Removed `express`/`cors`/`ws`/`playwright` from `core` deps; enabled `declaration` so electron consumes core types.
  - **electron**: `ipc/register.ts` now dispatches `pig:rpc` → `core.services[method]`, `pig:stream:open/close` for `agentRun|session|commandLog|fsWatch`, and `pig:terminal:*` PTY I/O. `env.ts` sets `PIG_ENV_FILE` to userData/.env.
  - **preload**: exposes `window.pig` (`rpc`, `streamOpen`, `terminalCreate`, `pickFolder`).
  - **renderer**: rewrote `lib/api.ts` onto `window.pig` (signatures unchanged); added `lib/pig.ts`; `useFsWatcher` + `Terminal.tsx` now use IPC instead of WebSocket; `BrowserPanel` is a placeholder until BrowserView lands; `exportChats` downloads via Blob URL.
  - All three workspaces build clean; Electron app launches (prod renderer loads, no crash).
  - **TODO next**: wire BrowserView controller into `browserSession`; package Windows NSIS.
- **2026-05-31** — Terminal on Windows: installed VS Build Tools, upgraded `node-pty` to `1.1.0`, rebuilt native addon (`npm run rebuild:native` with `SpectreMitigation=false`). Core loads PTY via `createRequire` (not ESM `import()`). **Do not** use `electron -e "..."` on PowerShell — it breaks launch paths.
- **2026-05-31** — Windows PTY default shell: `powershell.exe` (override with `PIG_SHELL`); terminal UI labels `PowerShell 1`, … instead of `bash`.
- **2026-05-31** — Editor tabs: VS Code–style dirty dot (replaces ✕ until hover), unsaved-close modal (Save / Don't Save / Cancel via `DialogProvider.unsavedChanges`), drag-and-drop tab reorder; editor no longer remounts on fs-watch `refreshKey` (uses separate `editorReloadKey`).
- **2026-05-31** — Editor hardening audit: `lib/editorBuffer.ts` (per-path buffers + dirty helpers); Monaco `isFlush` + CRLF-normalized dirty; per-path `reloadPath`/`reloadSeq` (fixes global reload wiping other tabs); tab/close dirty synced from buffer; hunk undo reloads only the affected file.
- **2026-05-31** — Browser panel: Electron `BrowserView` + IPC (`pig:browser:*`) + `BrowserDriver` in core; renderer toolbar (nav/back/forward/URL); agent `browser_*` tools work via `browserSession`.
- **2026-05-31** — BrowserView layout fix: bounds scaled by UI zoom in main, clamped to content size, `setTopBrowserView`; renderer `measureBrowserHost` + ResizeObserver on viewport/editor-area/center, `pig:browser:layout` on resize/zoom/maximize; browser tab wrapped in `.editor-pane--browser` with flex fill.
- **2026-05-31** — Browser panel parity with web sample: screenshot + add to chat, element inspect (in-page overlay via console bridge), page zoom in/out/reset, stop/launch, keyboard F5/Alt+arrows/Esc; IPC `pig:browser:screenshot|inspectStart|inspectStop|zoom|stop|focus` + `pig:browser:inspectPick`.
- **2026-05-31** — Browser select-element chips (Cursor-style): `ComposerEditable` contenteditable with inline pills (`<div>`, `<h3>` icon + tag); tokens `{{select el N}}` in storage; `ChatTurn.selectElMeta` for sent bubbles; removed duplicate select-el chips from composer header.
- **2026-05-31** — App icon: `icon.png` → `app/electron/resources/` + `app/renderer/public/`; `appIcon.ts` + `electron-builder` `buildResources`; `npm run sync-icon` copies from repo root.
- **2026-05-31** — BrowserView layout reset: `measureBrowserHost` clips viewport to `.editor-pane--browser` + `.center` + `.app` (no arbitrary width inset); zero bounds when measure fails; zoom scaling restored in main only when `zoomFactor !== 1`; tooltip only portals inside chat mount (no `position:fixed`).
- **2026-05-31** — Browser UX rework (VS Code-like): `BrowserView` is an OS-level overlay that can't layer under HTML, so the "More" actions moved from a full-width inline strip to a floating dropdown anchored under the ⋯ button. To make the dropdown visually overlay the page (native view can't be layered under HTML): on open, `BrowserPanel` captures a page screenshot, paints it as `.browser-frozen-shot` in the viewport, then renders a `[data-suppress-browser-overlay]` marker so `useBrowserOverlayGuard` zeroes the native view — the menu (toolbar `z-index`) overlays the frozen image. On close the marker drops (live view returns) and the screenshot lingers ~240ms to avoid a repaint flash. Added a real **Chrome DevTools toggle** (was confused with the terminal button): controller `browserToggleDevTools`/`browserDevToolsOpen` (detached devtools, closed on stop/detach), IPC `pig:browser:toggleDevTools` + `pig:browser:devToolsOpen`, preload + `pig.ts` wrappers, new `IconBug`, distinct toolbar button. Removed the disabled "Panel layout" button.
- **2026-05-31** — Agent browser tools fix: when agent called `browser_*` without the Browser tab open, `BrowserView` was 0×0 (hidden/suppressed) so pages didn't render and tools returned empty/errors. Added `browserEnsureForAgent()` (visible + fallback viewport + `pig:browser:agentActivate` to auto-open Browser tab), `browserHasPageLoaded()`, and `ensureStarted()` on every browser tool in executor. Prompts updated (Playwright → embedded Electron browser).
- **2026-05-31** — Browser black viewport fix: removed erroneous UI-zoom double-scale on bounds; call `setTopBrowserView` when panel bounds are valid (frameless transparent window on Windows); `agentOverlayLock` + renderer agent grace period so overlay guard doesn't zero the view during agent tab activation; restore default HOME navigation when page is still `about:blank`; transparent `.browser-viewport` background.
- **2026-05-31** — Browser floating overlay fix: removed `setTopBrowserView` (regression — centered BrowserView over whole UI on Windows). Main process now measures `.browser-viewport` bounds via `executeJavaScript`; rejects centered fallback-sized rects; `browserSetTabActive` IPC; clear bounds on overlay suppress / tab inactive.
- **2026-05-31** — Browser layout: migrated embedded page from deprecated `BrowserView` to `WebContentsView` on `win.contentView`; hide via `setVisible(false)` instead of 0×0 bounds; renderer `setBounds` IPC is a layout nudge only (main re-measures DOM); UI zoom divides DOM coords before `setBounds`.
- **2026-05-31** — Browser layout revert: `WebContentsView` on `win.contentView` misaligned on frameless Windows (black box over explorer). Back to **`BrowserView` + `addBrowserView`/`removeBrowserView`**; bounds from renderer `.browser-viewport` `getBoundingClientRect` only; hide by detaching view (no 0×0); no main-process DOM measure, no fallback center bounds, no `setTopBrowserView`; stop sending zero bounds during layout races; keep cached bounds when overlay/tab toggles.
- **2026-05-31** — Browser embed fix (final): replaced **`BrowserView` bounds overlay** with in-panel **`<webview>`** guest (`webviewTag: true`). Page paints inside the DOM viewport (no coordinate sync); main registers guest via `pig:browser:registerGuest(webContentsId)`; agent tools drive the same guest `webContents`. Removed bounds sync / `measureBrowserHost` from hot path.
- **2026-06-01** — `<webview>` layout and visibility crash fix on Windows: 
  - Restored the `<webview>` guest after a failed migration to `WebContentsView` (which had unfixable DPI scaling and maximize offset bugs on frameless transparent windows). 
  - Fixed the `<webview>` crashing / GPU buffer dropping when switching tabs by avoiding `display: none` or 1x1 resizing. The `.editor-pane--browser` and `.browser-webview--hidden` now use `opacity: 0`, `pointer-events: none`, `z-index: -1`, `position: absolute`, `left: 0`, `top: 0`, `width: 100%`, `height: 100%` to safely hide the `<webview>` without destroying its rendering context.
  - Fixed an infinite loop overlay bug where `<div data-suppress-browser-overlay>` triggered `useBrowserOverlayGuard` to permanently hide the browser when switching tabs.
  - Restored missing CSS `.browser-webview` (`flex: 1`, `width: 100%`, `height: 100%`) so it fills the viewport.
- **2026-06-01** — Agent intelligence pass: native OpenAI **tool calling** loop (`nativeToolLoop.ts`, `agentTools.ts`, `chatWithTools` in `client.ts`) default for cloud/OpenAI-compatible providers; ReAct fallback for Cursor/Ollama native and `LLM_DISABLE_NATIVE_TOOLS=1`. `read_file` supports `start_line`/`end_line`; compact `search_code` observations; `buildManifestExcerpt` on turn 1; efficiency prompts + `taskShape.ts` guardrails retained.
- **2026-06-01** — Agent v2: **ripgrep** search (`ripgrepSearch.ts`), match-centric previews (`smartPreview.ts`), two-pass file ranking, **parallel** native tool execution, `compactObservation` + `messagePrune` for token budget, browser tools in native schema, default **economical** prompt mode.
- **2026-06-01** — Agent v3 index + streaming: regex **symbol index** (`index/symbolIndex.ts`), optional **embedding** semantic search (`embeddingIndex.ts`, `llm/embeddings.ts`), `warmWorkspaceIndex` at run start; native tools `find_symbol`, `find_references`, `semantic_search`; **SSE streaming tool calls** (`chatWithToolsStream`, `toolStreamAccum.ts`) with early execution + `tool_payload_streaming` UI for large patches (`LLM_DISABLE_NATIVE_STREAM=1`, `LLM_DISABLE_SEMANTIC_INDEX=1`).
- **2026-06-01** — Parser stability: `normalizeReActBoundaries` for glued `}THOUGHT:` / `END"}THOUGHT:`; bare `{"patches":...}` at stream start (no `ACTION:`); `detectToolIntent` disabled when ReAct markers or bare tool JSON present; `write_patch` only scheduled when payload has `END`; `detectStreamingToolPayload` moved to `parser.ts`.
- **2026-06-01** — Native tool loop ReAct bridge: when API returns no `tool_calls` but content has `THOUGHT`/`ACTION: {...}` (common on local DeepSeek), parse via `parseAgentResponse` and execute tools instead of treating the blob as FINAL.
- **2026-06-01** — Settings UI **Agent tool mode** (`AGENT_TOOL_MODE`: auto vs react) persists `LLM_DISABLE_NATIVE_TOOLS=1` in user `.env` via `settingsSave`.
- **2026-06-01** — Agent context for static-site → React tasks: seed `index.html`/`style.css`/`script.js` into FILES previews (`rankRelevant`), manifest excerpts, migration hints (list_files ≠ content; never ask user to paste), stronger workspace-path mismatch guidance.
- **2026-06-01** — ChatGPT-style write refusals: stronger `AGENT_WRITE_AUTHORITY` + `[RUNTIME]` user prefix; system prompts identify Pig Agents Desktop; expanded `finalLooksLikePasteOnlyRefusal` + auto nudge (VI/EN paste-only patterns).
- **2026-06-01** — **ReAct is default** again: `agentToolMode.ts`, `AGENT_TOOL_MODE=react` in settings/.env; native `tool_calls` only when user picks **Auto** (experimental). UI copy updated; ReAct loop log line in runner.
- **2026-06-01** — **Removed native tool_calls** from product path: deleted `nativeToolLoop.ts`, `agentTools.ts`, `toolStreamAccum.ts`, `messagePrune.ts`; removed `chatWithTools*` from `client.ts`. Agent is ReAct-only; DSML parsing kept in `dsmlTools.ts` + `parser.ts`.
- **2026-06-01** — ReAct context + debug: `projectAnchors.ts` (package.json/tsconfig/README boost, `defaultEntryPaths`); `rankRelevant` merges semantic path scores after `warmWorkspaceIndex`; debug tasks get `preferDebugLines` previews + `taskShape` hint; `embeddingIndex` priority walk, boundary chunking, shorter excerpts, `semanticPathScores`; compact debug workflow in `prompt-compact.ts`; tighter `semantic_search` observations.
- **2026-06-01** — Chat log UX: structured `activity` SSE (prepare/index/context only); timeline renders `TraceActivityRow`/`TraceLogRow`/`TracePolicyRow` inline (removed ActivityRail + RunDetails); dropped Ant Design `Steps` for lightweight `trace-iteration-block`; live status in chat bar; kept command_chunk RAF + scroll tweaks.
- **2026-06-03** — Browser stability: idempotent `registerBrowserGuest` (same `guestId` no-op); `BrowserPanel` registers once per webview (no immediate + dom-ready double IPC; no re-register on every navigation); initial HOME navigate only once; `browserNavigate` cancels stale load listeners via generation counter; safer CDP attach when debugger already held.
- **2026-06-03** — Chat history delete fix: `chatDelete` updates index before unlink; `chatsList` prunes index rows without session files; renderer awaits delete, blocks saves for deleted ids, no empty ghost session on missing file; unload uses `flushSave` (removed broken sendBeacon to /api).
- **2026-06-03** — `run_command` tool preference (soft): detect shell read/search patterns (findstr/grep/node -e/…) and append observation tip + log nudge toward read_file/search_code; commands still run. Prompts say prefer agent tools, allow shell when genuinely better.
- **2026-06-03** — LLM vision guard: DeepSeek and other text-only endpoints no longer send `image_url` parts (fixes 400 deserialize error); `llmSupportsVision()` + text fallback note in user message; warn once in logs.
- **2026-06-03** — `run_command` live output: fixed event ordering (`command_chunk` was delivered sync while `action` was deferred via `setImmediate` / React `startTransition`, so chat often showed no streaming under Run). Session listeners now receive events in order; `action` appends synchronously in Chat; orphan `command_chunk` before action still render on replay.
- **2026-06-03** — Windows folder delete locks: `windowsReleasePathLocks` matches long + 8.3 paths, folder-leaf in cmdline, `taskkill /T`; PTY `kill` uses process tree; Electron kills terminal tabs whose cwd overlaps delete target via `setBeforeDeletePathHook`.
- **2026-06-03** — Data-loss hardening: block `rd`/`rmdir`/`Remove-Item -Recurse`/`rm -rf` in `run_command` (`destructiveShellGuard.ts`); deny-list Windows patterns; workspace cannot be drive root; `deleteEntry` cannot remove workspace root; safer `windowsReleasePathLocks` (no shallow leaf kill).
- **2026-06-03** — Agent `delete_path` tool: one workspace-relative path (no `*`, `**`, `..`, absolute drives); file or directory via `deleteAgentPath` → `deleteEntry` + `safeJoin`; `delete_file` alias; prompts updated.
- **2026-06-03** — Checkpoint disk blow-up fix: auto `auto-pre-run` / `auto-pre-restore` no longer fall back to full-workspace file copy under `~/.pig-agents/backups` (git-only or skip); file backups manual-only (max 3); expanded ignore list; `checkpointPurgeFileBackups` + `scripts/purge-checkpoint-file-backups.mjs`.
- **2026-06-03** — Cursor/Copilot-style rollback: per-chat **run-files** snapshots (`runSnapshots.ts`) under `~/.pig-agents/chats/<ws>/snapshots/<chatId>/<runId>/` — only files touched before write; auto checkpoint with `chatId` uses this instead of full copy; `chatDelete` purges snapshots + chat-tagged checkpoints; restore UI supports `backupType: run-files`.
- **2026-06-03** — Chat hang fix: background sessions now emit `session_ended` when `runAgentInBackground` finishes (`notifySessionEnded` + IPC subscribe callback); renderer `streamSession.done` also falls back on `final`/`error`/`aborted` so “Agent is working…” clears.
- **2026-06-05** — Log chronological rendering & streaming Markdown:
  - Modified `Chat.tsx` to include `final` events in `traceSteps` so finalized answers are rendered chronologically inside the step timeline.
  - Implemented inline rendering of streaming final text inside the timeline's active iteration (`streamThoughtIter`).
  - Removed the standalone legacy `displayedFinalText` container from the bottom of the assistant message.
  - Changed thought folds and streaming final text to render using the `<Markdown>` component during active streaming, instead of raw plaintext.
  - Updated `streamingFinalText` definition to support streaming raw text in "Ask" mode.
  - Fixed sorting logic in `traceSteps` to prioritize iteration sorting (`ia - ib`) strictly before timestamps, resolving duplicate/interleaving step logs.
- **2026-06-05** — search_code tool regex and shell escaping fix:
  - Added `-F` / `--fixed-strings` argument to `ripgrepSearch` to treat all query search terms as literal strings instead of regular expressions, preventing syntax crashes on code tokens.
  - Changed `spawn` options for the `rg` process to use `shell: false` instead of `shell: true`, resolving command line splitting issues on Windows.
- **2026-06-05** — write_patch EOL matching line ending (CRLF vs LF) fix:
  - Updated `applyPatch` in `patch.ts` to normalize all line endings (`before`, `block.search`, and `block.replace`) to LF (`\n`) during matching and string replacement.
  - Detected if the original file used CRLF line endings, and restored them to CRLF before writing it back to disk. This fixes continuous `WP_SEARCH_MISS` failures on Windows files.
- **2026-06-01** — Terminal + agent stop fixes:
  - Explorer delete no longer kills PTY when target is a sibling file under the same cwd (`terminalTouchesPath` only matches deleted dir or parent-of-cwd).
  - Agent abort: `sessionAbortAll`, immediate `aborted` event + approval cancel; runner races tool batch vs abort signal; Chat Stop resets UI immediately and aborts all workspace sessions; WorkspaceManager "Stop all" button.
- **2026-06-01** — Agent tool format: **XML-only** (`<tool name="…">` + CDATA). Removed JSON `ACTION:`, DSML (`dsmlTools.ts`), JSON salvage/parser paths. New `xmlTools.ts`, `toolParams.ts`, `agentFormat.ts`, `prompt-tools.ts`. Runtime env block (`runtimeEnv.ts`) injects OS/shell per turn. Streaming peek in renderer updated for XML.

- **2026-06-01** — Chat history wipe fix: `sessionRef` in Chat now reconciles with lazy-loaded `session` prop (`reconcileChatSessionRef`); `patchSession`/`runTask` sync before write so follow-up sends no longer replace full history with a single turn.
- **2026-06-01** — Stop running command no longer errors: `killAgentCommand` now also kills background servers tracked on the ring (not just `pending`), returns `{ok, reason}` instead of bool; `agentCommandKill` is best-effort (no thrown IPC error); Terminals `handleKill` resets on `pid-not-ready` so the user can retry.
- **2026-06-01** — Browser navigate hang guard: localhost/file navigations now use an 8s timeout and `did-stop-loading` fallback; `waitForPageSettled` has a 1.5s cap so `browser_navigate http://localhost:5500` cannot leave the agent stuck on `working...` indefinitely.
- **2026-06-01** — Stop now aborts in-flight early tools: `runner.ts` wraps early-scheduled tool waits in `raceAbort()` so Stop does not wait for a stuck `browser_navigate`/tool promise. `BrowserPanel` no longer auto-loads HOME on mount; agent-opened Browser tabs stay `about:blank` until the explicit `browser_navigate` tool runs, avoiding apparent app reload/racing navigation.
- **2026-06-01** — Browser select-el composer cleanup: Chat now disables incoming browser element picks while an agent run is active, calls `browserInspectStop()` on submit, clears `browserElementRefsRef` after capturing turn metadata/images, and ComposerEditable clears chip tooltip/dropdowns when value is emptied. `browserStopInspect()` now emits inspect-cancel so BrowserPanel UI turns off. Fixes stale selected element screenshot/chip reappearing in the composer after the run finishes.
- **2026-06-01** — Chat/session isolation: active backend agent session storage is now keyed by `workspace + chatId` instead of workspace-only; Chat reconnect filters running backend sessions by `backendSession.chatId === current chat id`; Electron `session_info` and renderer `AgentSession` types now carry `chatId`. Prevents a newly created chat from reconnecting to / inheriting context from a previous chat's running agent session.
- **2026-06-01** — Static preview + delete guard: `resolveBrowserNavigateUrl` resolves workspace-relative `index.html` → `file://` and auto-allows file://+localhost; `externalBrowserLaunchHint`/`runCommandOutcome` cover `start ""/explorer *.html`; runtime + prompts steer preview to browser_navigate. `delete_path` now blocked unless the CURRENT user message has explicit delete intent (`taskHasExplicitDeleteIntent`) or path was self-created this run (`ToolContext.userTask`) — stops weak models deleting the whole project on "thực hiện lại đi". Continuation detection extended (thực hiện lại / thực thi / tiếp tục) and hint forbids delete/rebuild.
- **2026-06-06** — Fix stop/abort when agent is running `run_command`. Previously Stop halted the LLM stream but the cmd child kept running. Fix: (1) `signal?: AbortSignal` on SmartCommandOptions + abort listener SIGKILL; (2) signal on ToolContext; (3) pass opts.signal from AgentRunOptions through toolCtx to runSmartCommand. Files: tools/smartCommand.ts, agent/executor.ts, agent/runner.ts.
- **2026-06-25** — Parallel sub-agents: `agent/orchestrator.ts` (worker-pool `runSubAgents`, read-only tool set, result aggregation) + `spawn_subagents` tool in `executor.ts` (parses `<subtasks>`/repeated `<task>`, cap 6, dynamic import). Prompt + `xmlTools.ts` hardened for both subtask formats. See [`docs/parallel-subagents-design.md`](parallel-subagents-design.md).
- **2026-06-25** — Explorer real-time like VS Code: `utils/watcher.ts` uses recursive `fs.watch` on Windows/macOS (native `ReadDirectoryChangesW`/FSEvents) so writes/adds in subdirectories appear without a manual reload.
- **2026-06-25** — Editor: offline **Format Document** (Prettier) via `lib/formatProvider.ts` registered on Monaco mount (`Shift+Alt+F` / `Ctrl+Shift+I` / right-click) for JS/TS/JSON/HTML/CSS/SCSS/LESS/Markdown/YAML. New `components/EditorMedia.tsx` adds an in-editor **image viewer** (base64 over a new `readFileBase64Svc`) and a Cursor-style **Markdown Preview ⇆ Source** toggle; `App.tsx` routes the editor pane by extension.
- **2026-06-26** — **Claude Code (SDK) provider**: `llm/claudeAgentClient.ts` drives Claude Code headlessly via `@anthropic-ai/claude-agent-sdk` `query()` (tools disabled, single turn, Pig Agents' system prompt → text generator). Auth = Anthropic API key if set, else local `claude login` subscription. Wired into `client.ts` provider switch; registered in `integrations.ts`/`profiles.ts`; Settings UI hides Base URL and fetches the live model list via `supportedModels()` (`claudeCliModelsSvc`) showing friendly labels (Sonnet 4.6 / Opus 4.8 / Haiku 4.5). Vision works (base64 image content blocks). Composer model pill warms the label map on mount so it shows the real label at startup.
- **2026-06-26** — Cursor-style chat edit + unified rollback: click a past user message to edit it in place (`UserMessageEditor` — a real composer with `ComposerEditable` + `ComposerHeader` add/remove/paste images), Enter re-runs. **Re-run / Edit-and-re-run / Restore** all revert the workspace to the turn's pre-run checkpoint **and** roll the chat back to that turn (reversible). Fixed `reconcileChatSessionRef` undoing the trim (now guarded by `updatedAt`) so re-run no longer appends onto stale history. Re-run also carries the turn's attached images (was dropping them).
